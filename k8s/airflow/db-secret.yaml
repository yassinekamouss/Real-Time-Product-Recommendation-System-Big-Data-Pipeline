apiVersion: v1
kind: Secret
metadata:
  name: airflow-db-conn
  namespace: default
type: Opaque
stringData:
  # Replace with actual connection string: postgresql+psycopg2://user:pass@host:port/db
  connection: "postgresql+psycopg2://airflow:airflow_pass@your-rds-endpoint:5432/airflow"
